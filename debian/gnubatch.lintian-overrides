# These libraries are only useful to the binaries in the same package, so there's no point
# splitting them out into a separate lib package
gnubatch binary: package-name-doesnt-match-sonames libgnubatch-client1 libgnubatch-curs1 libgnubatch-int1
gnubatch binary: non-dev-pkg-with-shlib-symlink
