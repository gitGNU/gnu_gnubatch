# Automatically added by dh_installinit
if [ -x "/etc/init.d/gnubatch" ]; then
	update-rc.d gnubatch defaults >/dev/null
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d gnubatch start || exit $?
	else
		/etc/init.d/gnubatch start || exit $?
	fi
fi
# End automatically added section
